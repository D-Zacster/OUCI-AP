"""
Take in raw email data and break down into desired information

Payment list elements:
    time
    category
    order number
    total payment amount
    purchaser name
    purchaser email
    
Registration list elements:
    time
    category

"""

# to find a substring between unique chracters:
# s[s.find("(")+1:s.find(")")] #Just change the characters between the quotations